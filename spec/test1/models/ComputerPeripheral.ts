import { DataObject } from '../../../data-object';
import { EdmMapping } from '../../../index';

@EdmMapping.entityType('ComputerPeripheral')
export default class ComputerPeripheral extends DataObject {
    
}